import theme, {
	COLORS,
	SIZES,
	// FONTS,
	darkTheme,
	lightTheme,
	selectedTheme,
} from './theme';
import icons from './icons';

const isSmall = SIZES.height < 720;

export {
	theme,
	COLORS,
	SIZES,
	// FONTS,
	darkTheme,
	lightTheme,
	selectedTheme,
	icons,
	isSmall,
};
